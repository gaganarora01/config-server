#spring.cloug.config.server.git.uri=${Home}/MarriottWorkspace/config-server
spring:
   profiles:
      active:native
   cloud:
      config:
        server:
           native:
              searchLocations: classpath:\\\\${user.home}\\common-config\\
server:
   port:8888  
